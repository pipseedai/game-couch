# Roadmap

## Now

- Keep the testable-stage plan in `docs/testable-stage-plan.md` as the implementation contract.
- Build the remote runner seam for `bigchoof` with fake/local tests before live SSH.
- Add remote `generic-screen` screenshot capture and artifact fetch.
- Make Discord couch-room posting feel good enough for one real session.
- Rehearse, then run, the first manual couch-session eval in `docs/evals/testable-couch-session.md`.

## Next

- Add hotkey support for player-triggered moments.
- Add richer OCR/visual parsing hooks if the first eval shows Pip lacks context.
- Add OpenRCT2 plugin spike.
- Explore sandbox “wheel mode” for reversible single-player games.

## Later

- WoW plugin with careful ToS/privacy boundaries.
- Console/capture-card generic plugin.
- Agent-requested peeks and session scrapbook views.

## Parking Lot

- Always-on ambient video.
- Multi-agent turn-taking experiments.
- Voice-channel awareness.
